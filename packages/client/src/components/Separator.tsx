/** A 1px rule on either axis. `@hanzo/ui` paints it from gui tokens, so this is
 * the primitive itself — there is nothing left for a wrapper to add. */
export { Separator } from '@hanzo/ui/primitives/Separator';
